# Manuscript compatibility paths

The submitted Supplemental Material refers to the historical development path
`algebraic_atlas/karlsson_product_regular_coverage/`. The publication-facing,
manifested certificate package now lives under
`certificates/product_exceptional/`.

The retained entry point at the historical path forwards to that canonical
package, so the exact command printed in the submitted paper continues to
work without duplicating certificate sources.
